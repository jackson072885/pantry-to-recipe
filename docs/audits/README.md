# Audit Artifacts

Repository audit outputs belong in this folder instead of the backend runtime root.

- keep historical audit notes here when they are worth preserving
- keep generated runtime DBs, debug DBs, and temporary verification outputs out of source control
